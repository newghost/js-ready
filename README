Check an object, when it's avaiable exec the function,

When the javascript package is loaded in asynchronous thread you can use it, etc:

$ready("jQuery.extend", function(){
    console.log("jQuery is ready!");
});